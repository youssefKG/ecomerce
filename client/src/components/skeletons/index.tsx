import ProductSkeleton from "./product";
import CardSkeleton from "./card";
import ReviewsSkeleton from "./Reviews";

// this function take two components (sekeleton of component and  and components it self ) and laod it render the skeleton of if the data is not laod and the components if is it laod

export { CardSkeleton, ProductSkeleton, ReviewsSkeleton };
